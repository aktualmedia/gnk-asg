name: Hourly Business and Market Update

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'scripts/update_feeds_v2.py'
      - 'scripts/update_macro_data.py'
      - 'data/news_config_v2.json'
      - 'data/blocked_news.json'
      - '.github/workflows/hourly-data-update.yml'

permissions:
  contents: write

concurrency:
  group: hourly-public-data-update
  cancel-in-progress: false

jobs:
  update-data:
    name: Refresh classified news and market intelligence
    runs-on: ubuntu-latest
    timeout-minutes: 14
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Update fresh news and digital assets
        run: python scripts/update_feeds_v2.py
      - name: Update Bitcoin, gold, Brent oil and USD comparison
        run: python scripts/update_macro_data.py
      - name: Commit refreshed public datasets
        run: |
          git config user.name "gnk-asg-data-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/news.json data/market.json data/btc_chart.json data/macro_market.json data/update_status.json
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "Automated hourly market intelligence update"
            git push
          fi
